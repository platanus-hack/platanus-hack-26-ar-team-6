{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://relevo.local/contracts/user_context_profile.json",
  "title": "UserContextProfile",
  "description": "Profile contract for a Relevo user whose stored context can be bootstrapped locally or queried by the server.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "user_id",
    "display_name",
    "voice",
    "domain"
  ],
  "properties": {
    "user_id": {
      "type": "string",
      "format": "uuid",
      "description": "Stable identifier for the human user and their context partition."
    },
    "display_name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable name shown in bootstrap roster entries."
    },
    "voice": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tone",
        "first_person",
        "signature_phrases"
      ],
      "properties": {
        "tone": {
          "type": "string",
          "minLength": 1
        },
        "first_person": {
          "type": "boolean",
          "description": "Whether generated answers may use first person when grounded in this user's stored context."
        },
        "signature_phrases": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 0,
          "uniqueItems": true
        }
      }
    },
    "domain": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "primary",
        "tags",
        "expertise_summary"
      ],
      "properties": {
        "primary": {
          "type": "string",
          "minLength": 1
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "expertise_summary": {
          "type": "string",
          "minLength": 20,
          "maxLength": 280,
          "description": "One or two precise sentences used in bootstrap roster entries and on-demand context answering."
        }
      }
    }
  },
  "examples": [
    {
      "user_id": "3a835ae5-3d9c-4a76-a170-a4c278b2f8b9",
      "display_name": "Jorf",
      "voice": {
        "tone": "Direct, pragmatic, and coordination-focused; challenges weak assumptions without derailing delivery.",
        "first_person": true,
        "signature_phrases": [
          "Let me ground this in the contract.",
          "The useful question is what this changes downstream."
        ]
      },
      "domain": {
        "primary": "Agent runtime prompts",
        "tags": [
          "agent-runtime",
          "profile-contracts",
          "prompting"
        ],
        "expertise_summary": "Owns the local assistant prompt, request-context tool contract, and on-demand context answer template."
      }
    }
  ]
}
